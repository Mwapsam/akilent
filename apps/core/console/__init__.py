"""The Operator Console: pages for the people who run Akilent, mounted at /manage/.

Separate from the client app (a business's own screens). Every page is operator-only
(``apps.core.utils.admin_required``) and every change is recorded (``apps.core.audit``).
"""
